class Point {
public:
    int x;
    int y;

    void set(int newX, int newY) {
        x = newX;
        y = newY;
    }
};

int main() {
    Point p;
    int arr[3] = {1, 2, 3};
    p.set(10, 20);
    if (arr[0] < arr[1]) {
        std::print("OK");
    } else {
        std::print("NO");
    }
    return 0;
}
